// enum é um tipo de dado que representa uma lista de CHAVE e VALOR
// a routes vai ficar fora da utils devido a importancia dela ser maior, precisar de uma atenção a mais
export enum Routes {
  LIST = '/api/list',
  EPISODE = '/api/episode',
}
